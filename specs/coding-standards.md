# Coding Standards

> Copy this file to `.claude/specs/coding-standards.md` in your project and fill it in.
> Skills use this for code reviews, PR reviews, and when generating new code.

## Naming Conventions

<!-- Examples: camelCase for variables, PascalCase for components, kebab-case for files -->
- Variables and functions:
- Types/Classes/Components:
- Files and directories:
- Database tables/columns:
- Constants:

## Code Style

<!-- Reference your linter/formatter config if it exists, or describe key rules -->
- Formatter: <!-- e.g., Prettier with 2-space indent, 100 char line width -->
- Linter: <!-- e.g., ESLint with Airbnb config -->
- Key rules: <!-- e.g., "no any in TypeScript", "prefer const over let", "no default exports" -->

## Import/Export Patterns

<!-- Examples: "named exports only", "barrel exports via index.ts", "no circular dependencies" -->
-

## Error Handling

<!-- Examples: "throw typed errors, never Error base class", "always return Result type, never throw", "use custom AppError class" -->
-

## Async Patterns

<!-- Examples: "async/await everywhere, no .then()", "use Promises, not callbacks", "explicit error handling on all awaits" -->
-

## Comments and Documentation

<!-- Examples: "JSDoc on all public functions", "inline comments for non-obvious logic only", "no commented-out code" -->
-

## File Structure

<!-- Describe where things go: e.g., "co-locate tests with source", "feature-based folders", "types in types/ directory" -->
-

## PR Standards

<!-- Examples: "all PRs need tests", "squash commits before merge", "PR title must reference a ticket" -->
- Require tests for new logic:
- Commit style: <!-- conventional commits, squash, etc. -->
- Review requirements:

## Things We Avoid

<!-- List anti-patterns, forbidden libraries, or patterns your team explicitly avoids -->
-
